PREFIX schema: <http://schema.org/>
CONSTRUCT { ?resource1 ?resource3 ?resource4. }
WHERE {
  VALUES ?resource1 {
    <https://tbbt.tv/sheldon-cooper>
  }
  ?resource1 ?resource3 ?resource4.
  FILTER(!(?resource3 = schema:knows))
  FILTER((ISIRI(?resource4)) || (ISLITERAL(?resource4)))
}